import {printElements} from "./data.js"
// Math
const add = (a, b) => {
    // printElements([1, 2]) // will throw error

    return a + b
};

function substract(a, b){
    return a - b
};




export { add, substract }